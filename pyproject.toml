[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "C4", "RUF"]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "head_of_site.settings"
python_files = "test_*.py"
